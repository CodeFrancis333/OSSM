OSSM Backend

Quick start (backend)

```bash
cd backend
npm install
npm run dev   # requires nodemon
```

Provides simple JSON endpoints:
- `/api/rebar`
- `/api/detailing`
- `/api/aisc`

Populate `backend/data/aisc_v16.json` with the full AISC dataset.
